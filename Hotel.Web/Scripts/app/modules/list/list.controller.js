var List;
(function (List) {
    var ListController = (function () {
        function ListController() {
        }
        return ListController;
    }());
    List.ListController = ListController;
})(List || (List = {}));
